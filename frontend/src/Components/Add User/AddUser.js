import React from "react";
import AppBar from '../Components/Appbar';

function addUser() {
    return (
        <div>
            <AppBar/>
            <h1>Add User</h1>

        </div>
        );
}

export default addUser;